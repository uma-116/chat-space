.side-bar
  .side-bar__user-name
    .innerbox
      .name
        = current_user.name
      .icon
        =link_to new_group_path do
          = icon('fas', 'edit', class: 'side-bar__btn--edit')
        =link_to edit_user_path(current_user)  do
          = icon('fas', 'cog', class: 'side-bar__btn--setting')

  .side-bar__group-list
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。

